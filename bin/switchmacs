#!/usr/bin/env zsh
rm ~/.emacs.d
if [ $switchmacs = "spacemacs" ]; then
    ln -s ~/dotfiles/config/emacs.d/ ~/.emacs.d > /dev/null 2>&1
    switchmacs="emacs"
    echo "Switched to emacs."
else
    ln -s ~/Documents/spacemacs/ ~/.emacs.d > /dev/null 2>&1
    switchmacs="spacemacs"
    echo "Switched to spacemacs."
fi
